{
  "name": "mk-plugin-digest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reference external plugin — instance surface + own scheduler (weekly digest).",
  "scripts": { "start": "node --experimental-strip-types src/main.ts" }
}
